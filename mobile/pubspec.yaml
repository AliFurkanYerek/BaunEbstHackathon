name: afet_koordinasyon_mobile
description: AfetKoordinasyon AI — cross-platform WebView shell for the web app.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0
  webview_flutter_android: ^4.3.0
  webview_flutter_wkwebview: ^3.17.0
  permission_handler: ^11.3.1
  shared_preferences: ^2.3.3
  file_picker: ^8.1.4
  audioplayers: ^6.1.0
  torch_light: ^1.1.0
  screen_brightness: ^2.1.2
  wakelock_plus: ^1.2.10
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
